# Local development / reference packages for the sandbox.
# In Docker (Dockerfile.airflow): Cosmos is installed into the Airflow env WITHOUT
# the [dbt-postgres] extra; dbt + elementary live in /opt/airflow/dbt_venv.
apache-airflow>=3.1.8
dbt-core==1.8.9
dbt-postgres==1.8.2
astronomer-cosmos==1.15.1
elementary-data[postgres]==0.16.1
psycopg2-binary>=2.9.9
